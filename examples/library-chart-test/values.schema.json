{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
    "appName": {
      "default": "test-app",
      "description": "Test values for parent chart",
      "title": "appName",
      "type": "string"
    },
    "environment": {
      "default": "production",
      "description": "Library chart values that should be merged at top level",
      "title": "environment",
      "type": "string"
    },
    "global": {
      "description": "Global values are values that can be accessed from any chart or subchart by exactly the same name.",
      "required": [],
      "title": "global",
      "type": "object"
    },
    "region": {
      "default": "us-west-1",
      "title": "region",
      "type": "string"
    },
    "replicas": {
      "default": 2,
      "title": "replicas",
      "type": "integer"
    }
  },
  "required": [
    "appName",
    "replicas"
  ],
  "type": "object"
}